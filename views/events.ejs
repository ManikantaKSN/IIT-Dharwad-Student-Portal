<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>College Events</title>
  <link rel="stylesheet" href="/css/events.css">
</head>
<body>
  <header>
    <a href="/home" class="home-icon">
      &#x1F3E0;
    </a>
    <h1>College Events</h1>
  </header>
  <div class="hero">
    <h2>Welcome to our Annual Events!</h2>
    <p>Explore our exciting lineup of events.</p>
  </div>
  <div class="container">
    <div class="search-filter">
      <input type="text" id="searchInput" placeholder="Search events...">
      <select id="filterSelect">
        <option value="">Filter by type...</option>
        <option value="Academic">Academic</option>
        <option value="Cultural">Cultural</option>
        <option value="Technical">Technical</option>
        <option value="Sports">Sports</option>
        <option value="Miscellaneous">Miscellaneous</option>
      </select>
    </div>
    <div id="eventContainer">
      <h1><u> Upcoming events: </u></h1>
      <% if (events.length > 0) { %>
        <% events.forEach(event => { %>
          <div class="event" data-type="<%= event.eventType %>">
            <h2><%= event.eventName %> (<%= new Date(event.eventDate).toDateString() %>)</h2>
            <div class="event-details">
              <p class="wrap-text">Description: <%= event.eventDescription %></p>
              <p>Type: <%= event.eventType %></p>
              <p>Venue: <%= event.eventVenue %></p>
            </div>
          </div>
        <% }); %>
      <% } else { %>
        <p>No events found.</p>
      <% } %>
    </div>
  </div>
  <div class="container">
    <div class="past-events">
      <h2>Past Events</h2>
      <ul id="pastEventsList">
        <!-- Past events will be dynamically added here -->
        <li>Annual Sports Day - February 20, 2024</li>
        <li>Cultural Fest - January 10, 2024</li>
        <li>Science Exhibition - November 5, 2023</li>
      </ul>
    </div>
  </div>
  <script src="/js/events.js"></script>
</body>
</html>